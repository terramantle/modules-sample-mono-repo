# eks-cluster

EKS cluster with managed node groups.

Published to Terramantle as `eks-cluster/aws`. Release a new version by tagging:

```bash
git tag eks-cluster-aws@1.0.1 && git push origin eks-cluster-aws@1.0.1
```

The section below is generated by `terraform-docs` via the pre-commit hook — do
not edit it by hand.

<!-- BEGIN_TF_DOCS -->
<!-- END_TF_DOCS -->
