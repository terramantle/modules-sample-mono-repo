# Module identity for the Terramantle registry.
# The VERSION is NOT here - it is computed from your Conventional-Commit
# history and recorded as the git tag <module-dir>@<version>. This file only
# supplies what the registry takes verbatim from the publish URL.
name: eks-cluster
provider: aws
description: EKS cluster with managed node groups
