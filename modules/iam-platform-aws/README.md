# iam-platform

IAM platform with EKS, RDS, VPC, S3 — showcases the registry dependency graph.

Published to Terramantle as `iam-platform/aws`. Releases are driven by your
commit messages — merge a Conventional-Commit change to `main` and CI versions,
publishes, and tags it for you:

```bash
git commit -m "feat(iam-platform): describe the change"   # minor bump
git commit -m "fix(iam-platform): describe the fix"       # patch bump
```

The section below is generated by `terraform-docs` via the pre-commit hook — do
not edit it by hand.

<!-- BEGIN_TF_DOCS -->
<!-- END_TF_DOCS -->
