# iam-platform

IAM platform with EKS, RDS, VPC, S3 — showcases the registry dependency graph.

Published to Terramantle as `iam-platform/aws`. Release a new version by tagging:

```bash
git tag iam-platform-aws@1.0.3 && git push origin iam-platform-aws@1.0.3
```

The section below is generated by `terraform-docs` via the pre-commit hook — do
not edit it by hand.

<!-- BEGIN_TF_DOCS -->
<!-- END_TF_DOCS -->
