# vpc

Simple VPC with public and private subnets.

Published to Terramantle as `vpc/aws`. Releases are driven by your
commit messages — merge a Conventional-Commit change to `main` and CI versions,
publishes, and tags it for you:

```bash
git commit -m "feat(vpc): describe the change"   # minor bump
git commit -m "fix(vpc): describe the fix"       # patch bump
```

The section below is generated by `terraform-docs` via the pre-commit hook — do
not edit it by hand.

<!-- BEGIN_TF_DOCS -->
<!-- END_TF_DOCS -->
