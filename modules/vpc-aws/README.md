# vpc

Simple VPC with public and private subnets.

Published to Terramantle as `vpc/aws`. Release a new version by tagging:

```bash
git tag vpc-aws@1.1.2 && git push origin vpc-aws@1.1.2
```

The section below is generated by `terraform-docs` via the pre-commit hook — do
not edit it by hand.

<!-- BEGIN_TF_DOCS -->
<!-- END_TF_DOCS -->
