# Module identity. The VERSION is NOT here — it comes from the git tag
# (e.g. vpc-aws@1.1.2). This file only supplies what the registry takes
# verbatim from the publish URL: name, provider, and a description.
name: vpc
provider: aws
description: Simple VPC with public and private subnets
