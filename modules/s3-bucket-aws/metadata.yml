# Module identity. The VERSION is NOT here - it comes from the git tag
# (e.g. s3-bucket-aws@1.0.1). This file only supplies what the registry takes
# verbatim from the publish URL: name, provider, and a description.
name: s3-bucket
provider: aws
description: S3 bucket with configurable ACL and CORS
