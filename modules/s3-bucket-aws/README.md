# s3-bucket

S3 bucket with configurable ACL and CORS.

Published to Terramantle as `s3-bucket/aws`. Release a new version by tagging:

```bash
git tag s3-bucket-aws@1.0.1 && git push origin s3-bucket-aws@1.0.1
```

The section below is generated by `terraform-docs` via the pre-commit hook — do
not edit it by hand.

<!-- BEGIN_TF_DOCS -->
<!-- END_TF_DOCS -->
