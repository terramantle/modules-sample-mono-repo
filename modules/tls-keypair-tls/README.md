# tls-keypair

ED25519 keypair plus a self-signed certificate. Cloud-free, applies instantly.

This module is **deliberately interesting for the state scanner**: Terraform
stores the generated private key verbatim in state, so when the consuming
stack's state is pushed to Terramantle, the `trufflehog` scanner should flag the
embedded private key. Use it to watch state-scanning catch a real secret.

Published to Terramantle as `tls-keypair/tls`. Releases are driven by your
commit messages - merge a Conventional-Commit change to `main` and CI versions,
publishes, and tags it for you:

```bash
git commit -m "feat(tls-keypair): describe the change"   # minor bump
git commit -m "fix(tls-keypair): describe the fix"       # patch bump
```

The section below is generated by `terraform-docs` via the pre-commit hook - do
not edit it by hand.

<!-- BEGIN_TF_DOCS -->
## Requirements

| Name | Version |
|------|---------|
| <a name="requirement_tls"></a> [tls](#requirement\_tls) | >= 4.0 |

## Providers

| Name | Version |
|------|---------|
| <a name="provider_tls"></a> [tls](#provider\_tls) | >= 4.0 |

## Modules

No modules.

## Resources

| Name | Type |
|------|------|
| [tls_private_key.this](https://registry.terraform.io/providers/hashicorp/tls/latest/docs/resources/private_key) | resource |
| [tls_self_signed_cert.this](https://registry.terraform.io/providers/hashicorp/tls/latest/docs/resources/self_signed_cert) | resource |

## Inputs

| Name | Description | Type | Default | Required |
|------|-------------|------|---------|:--------:|
| <a name="input_algorithm"></a> [algorithm](#input\_algorithm) | Key algorithm: ED25519, ECDSA, or RSA | `string` | `"ED25519"` | no |
| <a name="input_allowed_uses"></a> [allowed\_uses](#input\_allowed\_uses) | Allowed key uses for the certificate | `list(string)` | <pre>[<br/>  "key_encipherment",<br/>  "digital_signature",<br/>  "server_auth"<br/>]</pre> | no |
| <a name="input_common_name"></a> [common\_name](#input\_common\_name) | Certificate subject common name | `string` | `"demo.terramantle.dev"` | no |
| <a name="input_ecdsa_curve"></a> [ecdsa\_curve](#input\_ecdsa\_curve) | Curve used when algorithm is ECDSA | `string` | `"P256"` | no |
| <a name="input_organization"></a> [organization](#input\_organization) | Certificate subject organization | `string` | `"Terramantle Demo"` | no |
| <a name="input_rsa_bits"></a> [rsa\_bits](#input\_rsa\_bits) | Key size in bits when algorithm is RSA | `number` | `2048` | no |
| <a name="input_validity_period_hours"></a> [validity\_period\_hours](#input\_validity\_period\_hours) | How long the self-signed cert is valid, in hours | `number` | `8760` | no |

## Outputs

| Name | Description |
|------|-------------|
| <a name="output_cert_pem"></a> [cert\_pem](#output\_cert\_pem) | PEM-encoded self-signed certificate |
| <a name="output_private_key_pem"></a> [private\_key\_pem](#output\_private\_key\_pem) | PEM-encoded private key (sensitive - present in state for the scan demo) |
| <a name="output_public_key_openssh"></a> [public\_key\_openssh](#output\_public\_key\_openssh) | OpenSSH-formatted public key |
| <a name="output_public_key_pem"></a> [public\_key\_pem](#output\_public\_key\_pem) | PEM-encoded public key |
<!-- END_TF_DOCS -->
