# Module identity. The VERSION is NOT here — it comes from the git tag
# (e.g. rds-postgres-aws@1.0.0). This file only supplies what the registry takes
# verbatim from the publish URL: name, provider, and a description.
name: rds-postgres
provider: aws
description: RDS PostgreSQL instance
