# rds-postgres

RDS PostgreSQL instance.

Published to Terramantle as `rds-postgres/aws`. Releases are driven by your
commit messages — merge a Conventional-Commit change to `main` and CI versions,
publishes, and tags it for you:

```bash
git commit -m "feat(rds-postgres): describe the change"   # minor bump
git commit -m "fix(rds-postgres): describe the fix"       # patch bump
```

The section below is generated by `terraform-docs` via the pre-commit hook — do
not edit it by hand.

<!-- BEGIN_TF_DOCS -->
<!-- END_TF_DOCS -->
