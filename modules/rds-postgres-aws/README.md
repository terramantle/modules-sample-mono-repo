# rds-postgres

RDS PostgreSQL instance.

Published to Terramantle as `rds-postgres/aws`. Release a new version by tagging:

```bash
git tag rds-postgres-aws@1.0.0 && git push origin rds-postgres-aws@1.0.0
```

The section below is generated by `terraform-docs` via the pre-commit hook — do
not edit it by hand.

<!-- BEGIN_TF_DOCS -->
<!-- END_TF_DOCS -->
